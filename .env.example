#===============================================================================
# Vany CI/CD Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env file - it contains secrets!
#===============================================================================

#-------------------------------------------------------------------------------
# VM SSH Configuration (for deployment)
#-------------------------------------------------------------------------------

# Your VM's IP address or hostname
# Example: VM_HOST=203.0.113.50
VM_HOST=

# SSH username for your VM
# Example: VM_USER=ubuntu
VM_USER=

# SSH port (usually 22)
VM_SSH_PORT=22

#-------------------------------------------------------------------------------
# GitHub Secrets Guide
#-------------------------------------------------------------------------------
# You need to add these as GitHub repository secrets:
# Go to: Repository → Settings → Secrets and variables → Actions → New repository secret
#
# Required secrets:
#
# 1. VM_HOST
#    - Your VM's public IP address or hostname
#    - Example: 203.0.113.50
#
# 2. VM_USER  
#    - SSH username on your VM
#    - Example: ubuntu, root, or your username
#
# 3. VM_SSH_KEY
#    - Your SSH private key for connecting to the VM
#    - This is the PRIVATE key (not .pub)
#    - Generate with: ssh-keygen -t ed25519 -C "github-actions-deploy"
#    - Copy the ENTIRE content including -----BEGIN and -----END lines
#    - Add the .pub key to your VM's ~/.ssh/authorized_keys
#
# 4. VM_SSH_PORT (optional)
#    - SSH port if not default 22
#    - Default: 22
#
#-------------------------------------------------------------------------------
# How to Set Up SSH Key for Deployment
#-------------------------------------------------------------------------------
#
# Step 1: Generate a new SSH key pair (on your local machine)
#   ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy_key
#
# Step 2: Add public key to your VM
#   ssh-copy-id -i ~/.ssh/github_deploy_key.pub YOUR_USER@YOUR_VM_IP
#   
#   Or manually:
#   cat ~/.ssh/github_deploy_key.pub | ssh YOUR_USER@YOUR_VM_IP "cat >> ~/.ssh/authorized_keys"
#
# Step 3: Copy private key content for GitHub secret
#   cat ~/.ssh/github_deploy_key
#   
#   Copy EVERYTHING including:
#   -----BEGIN OPENSSH PRIVATE KEY-----
#   ... (the key content) ...
#   -----END OPENSSH PRIVATE KEY-----
#
# Step 4: Add to GitHub Secrets
#   - Go to your repo: https://github.com/behnamkhorsandian/Vanysh/settings/secrets/actions
#   - Click "New repository secret"
#   - Name: VM_SSH_KEY
#   - Value: Paste the entire private key
#
# Step 5: Add other secrets
#   - VM_HOST: Your VM IP (e.g., 203.0.113.50)
#   - VM_USER: Your VM username (e.g., ubuntu)
#
#-------------------------------------------------------------------------------
# Deployment Trigger
#-------------------------------------------------------------------------------
#
# To deploy to your VM, push a tag ending with "-lit":
#   git tag v1.0.1-lit
#   git push origin v1.0.1-lit
#
# The workflow will:
#   1. SSH into your VM
#   2. Pull the latest code
#   3. Restart the proxy service
#
#-------------------------------------------------------------------------------
